type Post {
  id: ID!
  createdAt: String
  updatedAt: String
  title: String
  content: String
  published: Boolean
}

type Profile {
  id: ID!
  bio: String
}

type User {
  id: ID!
  email: String!
  name: String
  posts: [Post]
  profile: Profile
}
